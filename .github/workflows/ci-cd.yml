name: RF Event Management CI/CD

on:
  push:
    branches:
      - main

jobs:

  docker:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: docker build -t tejas018/rf-backend ./backend

      - name: Push Backend Image
        run: docker push tejas018/rf-backend

      - name: Build Frontend Image
        run: docker build -t tejas018/rf-frontend ./frontend

      - name: Push Frontend Image
        run: docker push tejas018/rf-frontend

      - name: Build Admin Image
        run: docker build -t tejas018/rf-admin ./admin-app

      - name: Push Admin Image
        run: docker push tejas018/rf-admin